import Foundation
import Combine

/// Source of glanceable "live activities" (charging/battery, timers) shown when
/// the notch is idle. Implemented in Milestone 5.
@MainActor
final class ActivityModel: ObservableObject {
    // TODO: Milestone 5 — battery/charging monitor (IOKit) + timers.
}
